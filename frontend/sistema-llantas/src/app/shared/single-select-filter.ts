import {Component,ElementRef,EventEmitter,HostListener,Input,Output,ViewChild,computed,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';
import {MultiSelectOption} from './multi-select-filter';

@Component({selector:'app-single-select-filter',imports:[FormsModule],template:`
<div class="multi" [class.open]="open()"><b>{{label}}</b><button type="button" class="trigger" (click)="toggle($event)" [attr.aria-expanded]="open()" aria-haspopup="listbox"><span>{{summary()}}</span><i>{{open()?'▲':'▼'}}</i></button>@if(open()){<div class="popover" (pointerdown)="$event.stopPropagation()"><header><strong>{{label}}</strong><button type="button" (click)="close()">×</button></header>@if(options.length>6){<label class="search"><span>⌕</span><input #searchInput [ngModel]="search()" (ngModelChange)="search.set($event)" placeholder="Buscar…"></label>}<div class="commands"><button type="button" (click)="clear()">Limpiar</button></div><div class="options" role="listbox">@for(option of filtered();track option.value){<label [class.selected]="value===option.value"><input type="radio" [name]="label" [checked]="value===option.value" (change)="choose(option.value)"><span>{{option.label}}</span></label>}@empty{<p>No hay coincidencias.</p>}</div><footer><span>{{value?summary():'Sin filtro'}}</span><button type="button" (click)="close()">Listo</button></footer></div>}</div>
`,styleUrl:'./multi-select-filter.scss'})
export class SingleSelectFilter{@Input()label='';@Input()options:MultiSelectOption[]=[];@Input()value='';@Output()valueChange=new EventEmitter<string>();@ViewChild('searchInput')searchInput?:ElementRef<HTMLInputElement>;open=signal(false);search=signal('');filtered=computed(()=>{const term=this.search().trim().toLocaleLowerCase('es');return term?this.options.filter(x=>x.label.toLocaleLowerCase('es').includes(term)):this.options});constructor(private host:ElementRef<HTMLElement>){}toggle(e?:Event){e?.stopPropagation();this.open.update(x=>!x);if(this.open())setTimeout(()=>this.searchInput?.nativeElement.focus())}choose(value:string){this.value=value;this.valueChange.emit(value)}clear(){this.value='';this.valueChange.emit('')}summary(){return this.options.find(x=>x.value===this.value)?.label??'Todos'}close(){this.open.set(false);this.search.set('')}@HostListener('document:pointerdown',['$event'])outside(e:PointerEvent){if(this.open()&&!this.host.nativeElement.contains(e.target as Node))this.close()}@HostListener('keydown.escape')escape(){this.close()}}
